package com.poeticjustice.deeppoemsinc.service;

import org.springframework.stereotype.Service;

@Service
public class SubscriptionValidatorService {
    // Mock implementation; replace with actual subscription check (e.g., DB or external service)
    public boolean isSubscribed(String userId) {
        return true; // Simulate active subscription
    }
}